import React from "react";
import "./imageLoader.css"; // Optional: Add styles for loader

const ImageLoader: React.FC = () => {
  return <div className="image-loader">Loading image...</div>; // Customize with spinner, skeleton, etc.
};

export default ImageLoader;